server {
    index index.php index.html;
    server_name demo.convenantgemeenten.nl;
    error_log  /var/log/nginx/error.log;
    access_log /var/log/nginx/access.log;
    root /code;

	location /trouwplanner {
		alias /var/www/nested/public;
		try_files $uri $uri/ /index.php$is_args$args;

		location ~ \.php$ {
			try_files $uri =404;
			fastcgi_split_path_info ^(.+\.php)(/.+)$;
			fastcgi_pass php:9000;
			fastcgi_index index.php;
			include fastcgi_params;
			fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
			fastcgi_param PATH_INFO $fastcgi_path_info;
		}
	}
}

server {
    server_name api.convenantgemeenten.nl;
    
	location / {
		proxy_pass http://weddingplanner/api/;
	}
}

#server {
#    server_name ns.convenantgemeenten.nl;
#
#	location /api/ {
#		proxy_pass http://weddingplanner_ns/api/;
#	}
#}
